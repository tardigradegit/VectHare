{
  "name": "vecthare-tests",
  "version": "1.0.0",
  "description": "Automated tests for VectHare RAG extension",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "@vitest/ui": "^2.1.0",
    "jsdom": "^27.0.1",
    "vitest": "^2.1.0"
  }
}
